# Deployment assets

Reserved for reference Dockerfiles, Compose stacks and deployment guidance after the services exist. Do not expose scaffold endpoints as a running security gateway. M3–M5 will add loopback-only development defaults, health/readiness checks, explicit authentication, bounded requests, key mounts, isolated test servers, and topology/bypass tests. Production deployment will require a separate reviewed hardening guide.
