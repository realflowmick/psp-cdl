name: Specification issue
description: "RFC version and section, competing interpretations, proposed change, compatibility effects, and example vectors."
labels: ["specification"]
body:
  - type: textarea
    id: details
    attributes:
      label: Details
      description: "RFC version and section, competing interpretations, proposed change, compatibility effects, and example vectors."
    validations:
      required: true
  - type: checkboxes
    id: privacy
    attributes:
      label: Public report
      options:
        - label: This report contains only synthetic data and no private vulnerability details.
          required: true
